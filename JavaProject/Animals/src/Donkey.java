import java.time.LocalDate;

public class Donkey extends PackAnimal {
    public Donkey(String name, LocalDate birthDate, boolean saddle, int loadCapacity) {
        super(name, birthDate, saddle, loadCapacity);
    }
}
