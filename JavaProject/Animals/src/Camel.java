import java.time.LocalDate;

public class Camel extends PackAnimal {
    public Camel(String name, LocalDate birthDate, boolean saddle, int loadCapacity) {
        super(name, birthDate, saddle, loadCapacity);
    }
}