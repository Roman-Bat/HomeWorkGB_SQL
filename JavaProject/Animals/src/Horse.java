import java.time.LocalDate;

public class Horse extends PackAnimal {
    public Horse(String name, LocalDate birthDate, boolean saddle, int loadCapacity) {
        super(name, birthDate, saddle, loadCapacity);
    }
}
